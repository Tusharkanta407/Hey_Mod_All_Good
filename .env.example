# Local playtest: restart `npm run dev` after editing this file.
# Upload/production: use Devvit app settings instead (see below).
#
# Get a key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# After first playtest install, also set the encrypted app secret:
#   devvit settings set geminiApiKey
# (paste the same key when prompted)
